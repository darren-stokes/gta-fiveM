import json
import hashlib

def array_fun(model, manu,car_array):
    car_array[manu].append({model})


if __name__ == "__main__":
    m = hashlib.joaat()
    #car_array = {}
    #with open("raw_html.txt", "r") as rh:
        #for lines in rh:
            #cars = open("cars_out.txt", "a")
            #if "by" in lines:
                #split = lines.split("by")
                #manu = split[1].strip()
                #model = split[0].strip()
                #model.strip("-")
                #model.strip()
                #car_array.update({model.lower().strip() : manu.lower()})
    #print(car_array)               
#                
                ##array_fun(model, manu, car_array)

    
    
